[Unit]
Description=Backup diario da configuracao do pve01

[Timer]
OnCalendar=*-*-* {{ proxmox_config_backup_hour }}:{{ proxmox_config_backup_minute }}:00
Persistent=true
Unit=pve01-config-backup.service

[Install]
WantedBy=timers.target
